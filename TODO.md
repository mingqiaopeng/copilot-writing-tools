# TODO：Continue 端 .prompt 输出语义重构

## 背景

Continue.dev 支持 **Edit 模式**——模型输出特定格式的修改块（search/replace、diff），Continue 自动 Apply 到文件。这个模式不依赖模型的 function calling 能力，千问等本地模型也能用。

当前 `.continue/prompts/` 下的文件是从 Copilot SKILL.md 直译的，指令是"用 edit 工具直接修改文件"——这在 Copilot Chat 里管用（Copilot 提供内置编辑工具），但在 Continue + 本地模型环境下失败了：

- 本地模型没有 tool-calling → 收到"改文件"指令后手足无措，输出"已修改"然后停止，或试图输出全文中途断掉

## 重构目标

把所有 `.prompt` 文件的输出语义从"直接修改文件"改为"输出 Continue Edit 格式的修改块"：

- Continue 支持 unified diff 格式（`@@ -a,b +c,d @@` hunk 头 + `+`/`-`/` ` 行标记），`isUnifiedDiffFormat()` 检测通过后即时 Apply，所有模型可用
- 用户点击 **Apply** 按钮，修改自动应用到文件

## 核心原则：最小修改原则

**每次只输出被修改的部分，不输出未修改的全文。**

- 改了一个段落 → 只输出这个段落的 diff 块
- 改了三个段落 → 输出三个独立的 diff 块
- 只改了标题 → 只输出标题的 diff 块
- 选中了文本 → 只输出选中范围内的 diff 块
- 全文生效的修改（如统一风格）→ 才输出全文

这个原则既减少模型输出量（千问 35B 不会因长文本中断），也让 Continue Apply 更精准。

## 文件分类

### A 类：输出插入型 diff 块（4 个）

新增内容（中心句/金句/摘要/过渡句），标注插入位置，每个插入点一个 diff 块。

| # | 文件 | 重构方向 |
|---|------|---------|
| A1 | `写中心句.prompt` | 输出中心句列表 + 各段对应插入 diff 块（段X开头原句 → 段X开头中心句+原句） |
| A2 | `神来之笔.prompt` | 输出选定句子 + 插入位置 diff 块 |
| A3 | `摘要生成.prompt` | 输出摘要 + 文件开头/选中内容开头插入 diff 块 |
| A4 | `增加过渡.prompt` | 输出过渡句 + 各段落间插入 diff 块 |

### B 类：输出替换型 diff 块（修改已有内容）

| # | 文件 | 操作范围 | 输出粒度 |
|---|------|---------|---------|
| B1 | `拆分段落.prompt` | 指定段落 | 被拆分段落 → 拆分后段落的 diff 块（原段→拆分后多段） |
| B2 | `合并段落.prompt` | 指定段落 | 被合并段落 → 合并后段落的 diff 块（多段→一段） |
| B3 | `扩充篇幅.prompt` | 选中/全文 | 每段独立 diff，或全文一个 diff（仅输出被扩充的段落） |
| B4 | `缩减篇幅.prompt` | 选中/全文 | 同上，仅输出被精简的段落 |
| B5 | `简化修辞.prompt` | 选中/全文 | 仅输出有修改的句子/段落的 diff 块 |
| B6 | `增加修辞.prompt` | 选中/全文 | 同上 |
| B7 | `优化句式.prompt` | 选中/全文 | 仅输出句式有修改的句子的 diff 块 |
| B8 | `去除标签.prompt` | 选中/全文 | 仅输出有 Markdown 标记被移除的段落的 diff 块 |
| B9 | `统一风格.prompt` | 全文（唯一全文输出） | 全文修改，可输出全文（如全文过长也可输出逐段 diff） |
| B10 | `校对勘误.prompt` | 全文 | 逐条勘误各输出一个 diff 块（每条一行修改） |
| B11 | `标题优化.prompt` | 标题 | 输出建议供确认，用户确认后仅输出标题 diff 块 |

### C 类：无需修改文件（4 个）

纯分析/建议输出，天然兼容 Continue。

| # | 文件 | 说明 |
|---|------|------|
| C1 | `量化分析.prompt` | 量化分析报告，JSON + 对话展示 |
| C2 | `大纲生成.prompt` | 大纲生成，输出大纲供用户确认 |
| C3 | `段落重组.prompt` | 段落重组，输出方案供用户确认 |
| C4 | `传达提纲.prompt` | 传达提纲，结构化提取 |

## 重构步骤

### 第一步：确认 Continue Edit 格式

1. 在 Continue + 千问 35B 中做一次简单修改测试
2. 确认 unified diff 格式可被 Continue 识别（`@@ -a,b +c,d @@` + `+`/`-` 标记）
3. 确认多个独立 diff 块是否都能生成独立 Apply 按钮

### 第二步：A 类（插入型）逐个重构

每个文件：
1. 删除"用 edit 工具"、"直接修改文件"字眼
2. 改为"为每个插入点输出一个 diff 块：原文本→原文本+新内容"
3. 不输出未修改的全文
4. 测试：Apply 后新增内容正确插入

### 第三步：B 类（替换型）逐个重构

每个文件：
1. 删除"直接修改文件"字眼
2. 改为"仅输出有修改的部分，每个修改点一个 diff 块"
3. 遵循最小修改原则：改哪里就只输出哪里的 diff
4. 未修改的段落/句子不出现在输出中
5. `{{selectedCode}}` 模板变量保留

### 第四步：统一输出规范

所有修改类 `.prompt` 文件末尾统一加输出规范段落：
- 强制使用 unified diff 格式（````diff` 代码块，`@@ -a,b +c,d @@` hunk 头 + `+`/`-`/` ` 标记）
- 每个独立修改一个 diff 块
- 只输出被修改的内容，不输出未修改部分

### 第五步：回归验证

用千问 35B 逐一测试全部 18 个 `/` 命令：
- 模型正常输出 diff 块，不中途截断
- Apply 按钮出现且可正常应用
- 应用后内容正确，未修改部分不受影响

## 依赖

- Continue.dev 版本需支持 Edit/Apply 功能（当前最新版已支持）
- 模型无需 tool-calling 能力，只需能理解并输出指定格式的文本块
